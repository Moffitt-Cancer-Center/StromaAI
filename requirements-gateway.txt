# =============================================================================
# StromaAI — Gateway Requirements
# =============================================================================
# Dependencies for the FastAPI secure gateway (src/gateway.py).
#
# Install:
#   pip install -r requirements-gateway.txt
#
# Version pins are minimum requirements. Pin to exact versions for production
# deployments after integration testing.
# =============================================================================

# Web framework + ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Async HTTP client for backend proxy + JWKS fetch
httpx>=0.27.0

# JWT validation (RS256 / ES256) with JWKS key-set management
PyJWT[crypto]>=2.9.0
cryptography>=43.0.0
